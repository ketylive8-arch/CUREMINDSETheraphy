# WORKPLAN — מה צריך ליצור, לתקן ולשנות

> רשימת עבודה אחת: קוד + תוכן/עיצוב לפי אסטרטגיית לונדון (The Soke · Good Inside · "Direct but Poetic").
> עדיפויות: **P0** = חוסם השקה · **P1** = חשוב · **P2** = שיפור.
> סימון 🔑 = צריך ממני נכס/מפתח פעם אחת (תמונה, חשבון Grow, תוכן) — כל השאר אני עושה בעצמי.

---

## חלק א' — תיקוני קוד (מה שלא פועל / חסר)

### A. תשלום Grow — הקוד מוכן, לא חי (P0)
- [ ] **A1** לחבר `GROW_CHECKOUT_URL` ו-`GROW_WEBHOOK_SECRET` ב-Render (env). 🔑 (פרטי חשבון Grow)
- [ ] **A2** להתאים את סכמת חתימת ה-webhook לזו האמיתית של Grow (כרגע placeholder‏ `${eventId}:${userId}:${enrollmentId}`).
- [ ] **A3** Frontend: לחבר את מסך ה-paywall ל-`GET /api/checkout` — להציג מחיר/תנאים, ולטפל יפה ב-403 (עדיין ב-trial).
- [ ] **A4** בדיקת קצה-לקצה של תשלום אמיתי (checkout → webhook → מנוי פעיל) בסביבת test של Grow.

### B. קישוריות נתונים — הפער העיקרי (P1)
- [ ] **B1** להוסיף `user_id` + `enrollment_id` לטבלאות הישנות (checkins, mood_logs, protocol_progress, client_materials, client_goals, daily_tasks, grounding_sessions) — כדי שנתונים יופרדו per-enrollment, לא רק per-account.
- [ ] **B2** מיגרציה אנונימי→רשום: בעת התחברות/הרשמה למזג נתונים שנכתבו תחת `X-Device-Token` אנונימי אל ה-`accountId`.
- [ ] **B3** לאחד את שני שעוני ה-trial (‏`patient_profile.trial_start_at` מול `enrollment.trial_ends_at`) למקור אחד.

### C. תיקוני נכונות קטנים (P2)
- [ ] **C1** מסלולים שדורשים התחברות מחזירים 400 (חוסר X-Device-Token) במקום 401 נקי — לתקן סדר המידלוור.
- [ ] **C2** `TRIAL_HOURS` מוגדר אחרי `getAccessStatus` שמשתמש בו — לסדר מחדש לבהירות (עובד, אבל שביר).
- [ ] **C3** cache-busting ‏(`?v=`) מקובע ל-20260829 ב-index.html — להפוך לאוטומטי לפי תאריך deploy.

### D. Deploy ו-persistence (P0)
- [ ] **D1** Render: ‏`DB_FILE` להצביע לדיסק **קבוע** (למשל `/data/curemindset.db`) — אחרת הנתונים מתאפסים ב-redeploy.
- [ ] **D2** לוודא ש-`vercel.json` מפנה `/api` ו-`/uploads` ל-Render בלבד.
- [ ] **D3** אחרי merge ל-main — לוודא ש-Vercel ו-Render נפרסו מה-commit הנכון.

### E. לוודא/להשלים מה שלא נבדק חי (P1)
- [ ] **E1** מוח ה-AI: לוודא שמפתח OpenAI פעיל ב-Render (אחרת רץ fallback מקומי). 🔑 (מפתח OpenAI)
- [ ] **E2** אודיו: לוודא TTS במכשיר + תשתית להעלאת MP3 עובדת.
- [ ] **E3** Back-Office (admin): לוודא כניסה, ניהול קודים, וחיווי סטטוס AI.
- [ ] **E4** הרשמה לסדנאות (`workshop_signups`) — לוודא שנשמר ומגיע ל-CRM/מייל.

---

## חלק ב' — תוכן ועיצוב לפי אסטרטגיית לונדון

### F. שפה חזותית — פרימיום שקט (P1)
- [ ] **F1** להוסיף 3 טוקני accent פסטל (תכלת `#DCE9EE`, צהוב חם `#F7EBCF`, ורוד-חול `#EFE1DA`) — הזהב נשאר הגיבור, פסטל רק כרקע/מבטא.
- [ ] **F2** Serif עדין (Playfair/Cormorant) לכותרות אנגלית בלבד + הרבה יותר white space.
- [ ] **F3** צילום: להחליף stock של חיוכים ב-macro (טבע/מרקם), פורטרט editorial ("מבט חושב"), ותמונות "מרחב" (אור/חלונות). 🔑 (תמונות ממני)

### G. ארכיטקטורת מסרים — "כלים לכל אתגר" (Good Inside) (P1)
- [ ] **G1** סקשן רשת כרטיסים מבוסס-כאב בדף הבית: כל כרטיס = אתגר של ההורה/מתבגר ← כלי (למשל "הילד מתפרץ" ← ויסות רגשי).
- [ ] **G2** Hero "Direct but Poetic": כותרת *"המרחב שבין השקט לדיבור"* + פורטרט editorial. 🔑 (תמונה)

### H. הפרדת מוצרים ושקיפות (בהשראת Curable/London) (P1)
- [ ] **H1** להפריד 3 מוצרים ברורים, לכל אחד CTA + מחיר + מסלול הרשמה נפרד:
  1. תוכנית דיגיטלית אישית (AI + אודיו) — self-serve, ₪297/חודש גלוי.
  2. סדנאות קבוצתיות (נוער/הורים/ארגונים) — "החל מ-" או שיחת התאמה.
  3. ליווי אישי / פרימיום — שיחת היכרות.
- [ ] **H2** שקיפות מחיר: להציג מחיר + תדירות **לפני** checkout במוצר הדיגיטלי.
- [ ] **H3** דף סדנה מלא לפי הסדר: למי מתאים · מה תחווה · מה תלמד/תתרגל · איך נראה המסע · מה כלול/לא כלול · מי מנחה · גיל/משך/מס' מפגשים · מחיר/הצעה · שאלות נפוצות · גבולות השירות · CTA. 🔑 (פרטי סדנאות ומחירים ממני)

### I. מעבר תוכן — קול "ישיר אך פואטי" (P2)
- [ ] **I1** יישור כל הטקסטים לקול חם, מכיל, לא מאשים, בלי הבטחת ריפוי.
- [ ] **I2** onboarding/auth שנפתח בשיחה אמפתית שממפה צורך ומחברת לתוכנית (לא טופס יבש).
- [ ] **I3** provenance לכל מודול AI: מקור, גרסה, קהל יעד, מטרת שינוי, גבולות בטיחות, תרגיל, פעולה קטנה, שאלת check-in. 🔑 (אישור מקורות/קבצים ממני)

---

## סדר מומלץ להתחיל
1. **P0 תשתית:** D1 (דיסק קבוע) → A1+A3 (חיבור תשלום) → E1 (מפתח AI).
2. **P1 חוויה:** G1 ("כלים לכל אתגר") → H1/H2 (הפרדת מוצרים + שקיפות מחיר) → F1/F3 (פסטל + צילום).
3. **P1 נתונים:** B1→B2→B3.
4. **P2 ליטוש:** F2, I1–I3, C1–C3.
