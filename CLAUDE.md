# CureMindset — כללי עבודה עם קטי (Kety Segev)

## כלל־על מוחלט (חובה, בכל שיחה)
**אני עושה הכל בעצמי. לעולם לא מבקש מקטי ללכת לשום מקום, ללחוץ על כלום, "לאמת", "לבדוק", או "לתקן" בעצמה.**
- קטי בונה את האתר *יחד* איתי — היא מכוונת, אני מבצע. היא לא הטכנאית.
- אני סורק, אני מאבחן, אני מתקן, אני מחבר, אני בודק, אני מעלה לאוויר.
- אם דרוש מפתח/הרשאה שרק לה יש (GitHub token, Render API key, Google) — אני מבקש את **המפתח עצמו פעם אחת** ואז עושה את כל השאר בעצמי. לעולם לא שולח אותה לבצע שלבים בממשקים.
- אם משהו באמת דורש פעולה ידנית שלה — אני מוצא דרך לעשות אותו בעצמי קודם; רק אם באמת אין ברירה, אני עושה את המקסימום ומשאיר לה את המינימום המוחלט, בלי "שיעורי בית".
- כל תשובה בעברית.

## עקרון "מוסיפים, לא בונים מחדש"
כל מה שקטי מבקשת הוא **תוספת** — לא לשבור, לא לבנות מחדש את מה שכבר עובד. משנים רק את מה שהתבקש במפורש.

## Deploy
- כל שינוי נבדק בדפדפן אמיתי (Playwright + chromium) לפני העלאה.
- Push ישיר ל-`main` דרך github.com עם הטוקן (עוקף את ה-proxy הפנימי שמחזיר 403).
- אחרי push: `git update-ref refs/remotes/origin/main <sha>` כדי לסנכרן את מצביע־המעקב.
- Vercel מגיש את ה-frontend הסטטי; Render (`curemindsetheraphy`) מריץ את ה-backend. vercel.json מפנה `/api` ו-`/uploads` ל-Render בלבד.

## ארכיטקטורה בקצרה
- Frontend: React 18 UMD + Babel-in-browser (בלי build). קבצים: app.js, memberArea.js, workshops.js, resilienceDashboard.js, adminApp.js, main.js.
- Backend: Express + node-sqlite3-wasm (server/).
- AI: server/openai.js (GPT) עם fallback מקומי server/guidedReply.js ("לא־מת"). ידע RAG: server/knowledge_base/*.md.
- אזור אישי: 5 טאבים של מערכת CureMindset (צ'ק-אין, מדד חוסן, החומרים שלי, משימות יומיות, התוכנית שלי). שלבי הפרוטוקול הישן (עוגן/גבול/קרקוע) מוסתרים מהמשתמש.
