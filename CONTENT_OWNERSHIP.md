# CONTENT_OWNERSHIP.md — חלוקת אחריות בין הסוכנים

**מטרה:** למנוע התנגשויות. בפרויקט הזה עובדים שני סוכנים — **Elara** (תוכן, אסטרטגיה ושיווק של קטי) ו-**Claude** (קוד, עיצוב ומימוש). המסמך הזה הוא מקור האמת למי אחראי על מה.

**עודכן:** 2026-09-15 · אושר על ידי קטי בשיחה.

---

## מקורות האמת לתוכן (לפני כל כתיבה)

1. **המודל הקליני:** פרוטוקול CURE בן 4 השלבים — `server/knowledge_base/18-cure-process-architecture.md`. שום טקסט באתר לא מציג מודל מתחרה או נוספאות של השלבים.
2. **עמוד השדרה של השיטה:** "מפת השיטה — CureMindset" ב-Google Drive של קטי (תיקיית 01 — השיטה והמודל). מקור לעקרונות, לקהלים ולמסע.
3. **תוכן הסדנאות:** `scripts/workshops-content.js` — כל שינוי תוכן עובר דרך הסכימה ומרונדר עם `node scripts/gen-workshops.js`. לא עורכים את ה-HTML המחולל ידנית.

---

## Elara — תוכן, אסטרטגיה ושיווק

**אחראית על:** מיצוב ואסטרטגיית מסר · היררכיית תוכן וארכיטקטורת מידע · קופי שיווקי בכל העמודים · חיבור השיטה לתוכניות · מפת SEO ותוכן פילרים · חומרי מכירה ומצגות (Drive).

**אופן העבודה:** תמיד בענף משלה (`content/*` או `docs/*`) + PR עם הסבר מלא על מה השתנה, למה, ומה לא נגע. **לא ממזגת לבד ולא דורכת על main.**

## Claude — קוד, עיצוב ומימוש

**אחראי על:** מימוש UI/UX לפי `DESIGN_TOKENS.md` ו-`CLAUDE.md` · backend, DB, אבטחה, ביצועים, נגישות · פריסה ותחזוקה של המערכת הדיגיטלית והבוט.

**אופן העבודה:** מממש PR-ים של תוכן שמגיעים מ-Elara (מבחינת עיצוב ותקינות), לא משכתב קופי לבד. אם מזהה בעיית תוכן — פותח Issue או מעיר בביקורת של ה-PR, לא משנה בעצמו.

---

## כללים משותפים

- **כל שינוי תוכן משמעותי = PR.** אין עריכות שקטות של טקסטים שיווקיים על main.
- **אישור סופי — קטי.** אף מיזוג של שינוי תוכן בלי שקטי ראתה.
- **טרמינולוגיה קבועה:** שיטת CureMindset · פרוטוקול CURE · "קטי הדיגיטלית" (לא "בוט AI" בפנייה ללקוח) · "שיחת היכרות" כ-CTA מרכזי.
- **אסור בתכלית:** הבטחות ריפוי, המצאת עדויות/מספרים/תוצאות, כותרות גנריות ("המסע שלך מתחיל כאן"), מיתוג מחדש של השיטה.
- **פרטים שממתינים לקטי** (סעיף 5 ב-DECISIONS.md): הסיפור האישי, עדויות בשם, טקסטים מאושרים ליחידות — לא למלא בעצמנו, לא משני הצדדים.

---

## פתוח כרגע (לעדכון במיזוג)

- **PR #1** (`content/method-marketing`): חיבור פרוטוקול CURE ל-3 עמודי הסדנאות + סקשן "איפה פוגשים את השיטה" ב-`/method`. ממתין לאישור קטי ומיזוג.
- **PR #2** (המסמך הזה): ממתין לאישור קטי ומיזוג.
- **בקנה אחד:** מיפוי פערים מול עמוד הבית (ה-5/15/30 שניות) → ארכיטקטורת עמוד בית חדשה עם קופי מלא → מפת SEO.
