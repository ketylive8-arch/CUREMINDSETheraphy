# DESIGN_STRATEGY_V2 — CureMindset (בהשראת Dr. Joe Dispenza)

> אסטרטגיה חדשה לבקשת קטי. הכיוון הקודם (Good Inside / כותרות ענקיות) נפסל.
> עיקרון־על: **ריסון, שקט, וקולנועיות.** להציג את **תוכנית ה-Mindset** — לא "אתר מטפלת".
> נשמר: מיתוג זהב-קרם, לוגו, RTL, עברית. משתנה: הטיפוגרפיה, ההיררכיה, האווירה.

---

## 1. מה מנצח אצל Dispenza (וזה מה שנאמץ)

| אלמנט | אצל Dispenza | אצלנו |
|------|--------------|-------|
| כותרות | **קטנות ומעודנות**, משקל בינוני/קל — לא צועקות | להקטין דרמטית את הכותרות; משקל 600, לא 800 |
| אווירה | קולנועית, רגועה, מרווחת, "מדע + שקט" | הרבה אוויר, פחות עומס, פחות דקורציה |
| תמונות | Full-bleed קולנועי, אטמוספרי | תמונה גדולה ונקייה, בלי "badge" משחקי |
| טיפוגרפיה | Sans נקי + labels קטנים ב-uppercase עם letter-spacing | Rubik מרוסן + eyebrow קטן ומרווח |
| מבנה | **Program-forward** — התוכניות הן הגיבור | להציג את תוכנית CureMindset בבירור |
| כפתורים | מעודנים, לפעמים outline, לא "שמנים" | CTA נקי ומדוד |
| צבע | ניטרלי/רגוע, מבטא עדין | קרם רגוע + זהב כמבטא בלבד (לא רקעים צבעוניים) |

## 2. טיפוגרפיה חדשה — סקאלה מוקטנת (הבקשה המרכזית)

```
Hero H1:      clamp(1.9rem, 3.2vw, 2.9rem)   weight 600   (היה 4.15rem/800!)
Section H2:   clamp(1.5rem, 2.4vw, 2.1rem)   weight 600
H3:           1.15rem                         weight 600
Body:         16–18px                         weight 400–500
Eyebrow:      12–13px  UPPERCASE  letter-spacing .18em  weight 600
```
כלל: **כותרת לא ממלאת חצי מסך.** נותנים לטקסט לנשום, לא להשתלט.

## 3. פלטה — ריסון

- רקע: קרם רגוע `#FBF9F5` / לבן `#FFFFFF`. **בלי blobs פסטליים** — Dispenza נקי.
- זהב מבטא בלבד: `#C2974A` / עמוק `#8A6A2F` — לקווים, CTA, labels. לא רקעים.
- טקסט: דיו `#221F1A` / משני `#5B554D`.
- הפרדת סקשנים בעדינות: קו דק / שינוי גוון קרם עדין, לא צבע.

## 4. מבנה עמוד הבית — Program-forward

הסיפור: **"CureMindset — תוכנית ה-Mindset."** לא רשימת בעיות, אלא תוכנית מובנית:

1. **Hero** — כותרת מעודנת + משפט אחד + CTA שקט + תמונה קולנועית.
2. **התוכנית** — מה זו CureMindset כתוכנית: פרוטוקול CURE + הרכיבים (ליווי / דיגיטלי / סדנאות) כ-3 אבני-בניין נקיות עם תמונה לכל אחת.
3. **השיטה (CURE)** — 4 שלבים, מוצג ברוגע (טיימליין/מספרים עדינים).
4. **למי** — מוצג מרוסן (שורה, לא 4 כרטיסים צבעוניים).
5. **קטי** — פורטרט אדיטוריאלי + פסקה שקטה.
6. **התחלה** — CTA אחד ברור (שיחת היכרות / התחלת התוכנית).

## 5. עקרונות מנחים
- **פחות זה יותר.** אם אפשר להסיר אלמנט — מסירים.
- **תמונה אחת חזקה** עדיפה על שלושה אייקונים.
- **כותרת קטנה + הרבה אוויר** = יוקרה. כותרת ענקית = רועש.
- כל סקשן: פעולה אחת, מסר אחד, מרווח נדיב.
- אנימציה: fade עדין בלבד. בלי "קפיצות".

## 6. מה מיישמים קודם (סדר)
1. **להקטין את כל הכותרות** לסקאלה החדשה (הבקשה המפורשת). ← מתחילים כאן.
2. לנקות את ה-Hero: להסיר badge ו-blobs, כותרת מעודנת, אווירה רגועה.
3. להפוך את סקשן הדיגיטלי + הסדנאות ל-"התוכנית" (program-forward) עם תמונות נקיות.
4. לרסן את שאר הסקשנים (פחות כרטיסים, יותר אוויר).
